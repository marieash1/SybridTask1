1. GET DATA FROM Api
2. Count of universities in each country

3. Min and Max universities 

{
    "country": "United States",
    "domains": [
      "lssu.edu"
    ],
    "state-province": null,
    "web_pages": [
      "http://www.lssu.edu/"
    ],
    "name": "Lake Superior State University",
    "alpha_two_code": "US"
  }

  
            {/* Not included */}
            {/* <div className="flex justify-around"> */}
                {/* <div className="bg-gray-50 text-black max-w-screen-sm rounded-lg m-8 p-9">
                    <h1 className="font-bold text-3xl text-blue-600 pb-3 text-center">Min University Count - {min}</h1>
                    <ul className="list-disc list-outside p-7 flex flex-wrap justify-around">
                        {arr.length > 0 ? (arr.map((country, index) =>
                            <li key={index} className="w-1/3 p-2">{country}</li>)) : null}
                    </ul>
                </div> */}
                {/* <div className="bg-gray-50 text-black p-9 max-w-screen-sm max-h-40 rounded-lg m-8 flex flex-col justify-center">
                    <h1 className="font-bold text-3xl text-blue-600 pb-3 text-center">Max University Count - {max}</h1>
                    <h2>{maxCountry} has {max} Universities</h2>
                </div>
            </div> */}